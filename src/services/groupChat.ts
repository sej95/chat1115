import debug from 'debug';

// Create debug logger
const log = debug('lobe-group-chat:service');

export class GroupChatService {
  // TODO: Implement group chat service
  public makeDecision = async () => {
    log('Making decision with messages: %O');
  };
}
